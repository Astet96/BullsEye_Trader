services:
  postgres:
    container_name: fed_crawler_db
    image: postgres:latest
    restart: unless-stopped
    environment:
      POSTGRES_DB: fed_crawler_db  # TODO: use secrets for db_name
      POSTGRES_USER: fed_crawler  # TODO: use secrets for user
      POSTGRES_PASSWORD: pass  # TODO: use secrets for password
      PGPORT: 5433
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - bullseye_trader

  api:
    container_name: fed_crawler
    stdin_open: true
    tty: true
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile_api
    volumes:
      - .:/fed_crawler
    depends_on:
      - postgres
    networks:
      - bullseye_trader

networks:
  bullseye_trader:
    name: bullseye_trader
    external: true
